<div class="flex flex-col gap-8">
  <div class="flex items-center justify-between">
    <h1 class="text-4xl font-bold"><%= link_to @podcast.name, podcast_path(@podcast), class: "text-red-600" %> Subscribers</h1>
  </div>
  <div id="users" class="grid min-w-full gap-4 2xl:grid-cols-4 xl:grid-cols-4 lg:grid-cols-2">
    <%= render @users %>
  </div>
</div>
