<div class="flex flex-col w-full gap-8">
  <div class="flex items-center justify-between">
    <h1 class="text-4xl font-bold">Community (<%= @users.count %>)</h1>
  </div>
  <div id="users" class="grid min-w-full gap-4 2xl:grid-cols-4 xl:grid-cols-4 lg:grid-cols-2">
    <%= render @users %>
  </div>
</div>
